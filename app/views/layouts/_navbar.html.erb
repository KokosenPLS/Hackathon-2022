<nav class="navbar navbar-light bg-light">
  <div class="container-fluid py-2">
    <h3 style="font-family: 'Dancing Script', serif" class="px-2"><%= link_to "RentAHut", root_path, class:"navbar-brand"%></h3>
    <div class="nav">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">
            <span class="material-icons" style="color:white">account_circle</span>
          </a>
          <ul class="dropdown-menu dropdown-menu-end settings-tab">
            <li><%= link_to "Endre konto instillinger",  edit_user_registration_path, class: "dropdown-item"%></li>
            <li class="dropdown-item">
              <%= button_to "Logg ut", destroy_user_session_path, method: :delete %>
            </li>
          </ul>
        </li>
      <% else %>
        <%= link_to "Logg inn", new_user_session_path, class: "nav-item btn btn-info" %>
      <% end %>
    </div>
  </div>
</nav>