<%= form_with(model: comment) do |form| %>
  <% if comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "New comment", style: "display: block" %>
    <%= form.text_area :content, class:"w-100 h-25"%>
  </div>

  <div style="display: none">
    <%= form.number_field :product_id, class:"w-100 h-25", value:comment.product.id %>
  </div>

  <div style="display: none">
    <%= form.number_field :user_id, class:"w-100 h-25", value:current_user.id%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
